#!/bin/sh
@X_CONFIG_DIR@
topdir=
curdir=.

if [ -f makefile ]; then 
    echo mv -f makefile makefile.bak
    mv -f makefile makefile.bak
fi

args="-DUseInstalled "$configdirspec
echo imake $args
imake $args -s makefile
